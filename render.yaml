services:
  - type: web
    name: wheelock-backend
    env: python
    plan: free
    runtime: python
    buildCommand: "pip install -r backend/requirements.txt"
    preDeployCommand: "echo '>>> Starting database seed...' && cd backend && python seed_database.py && echo '>>> Database seed complete'"
    startCommand: "cd backend && uvicorn main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: wheelock-postgres
          property: connectionString
      - key: ALLOWED_ORIGINS
        value: https://wheelock-frontend.onrender.com

  - type: web
    name: wheelock-frontend
    runtime: static
    staticPublishPath: ./frontend/dist
    buildCommand: "cd frontend && npm install && npm run build"
    routes:
      - type: rewrite
        source: "/*"
        destination: /index.html

databases:
  - name: wheelock-postgres
    plan: free